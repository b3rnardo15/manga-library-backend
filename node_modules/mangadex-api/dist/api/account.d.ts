import { UserResponse } from '../../types/data-types/user';
import { ApiResponse } from '../../types/response';
import { ApiBase } from './base';
export declare class AccountResolver extends ApiBase {
    static activate(code: string): Promise<boolean>;
    /**
     * @param username 1-64 characters
     * @param password 8-1024 characters
     * @param email
     */
    static createAccount(username: string, password: string, email: string): Promise<UserResponse>;
    static resendActivation(email: string): Promise<ApiResponse<Record<string, never>>>;
    static recover(email: string): Promise<ApiResponse<Record<string, never>>>;
    static completeRecover(code: string, newPassword: string): Promise<ApiResponse<Record<string, never>>>;
}
