import { RequestResult, ResponseTypeMap } from 'smol-request';
import { AgentOptions, MRequestOptions, Session } from '../types/agent';
export declare const DefaultOptions: {
    apiHost: string;
};
export declare class Agent {
    session?: Session;
    apiHost: string;
    constructor({ apiHost, session }?: AgentOptions);
    setSession(session: Session): void;
    saveSession(path: string): Promise<boolean>;
    static saveSession(path: string, session: Session): Promise<boolean>;
    loginWithSession(path: string): Promise<boolean>;
    call<K>(url: string, options?: MRequestOptions, body?: Record<string, unknown>): Promise<RequestResult<ResponseTypeMap<K>['json']>>;
    static call<T>(url: string, options?: MRequestOptions, body?: Record<string, unknown>): Promise<RequestResult<ResponseTypeMap<T>['json']>>;
}
